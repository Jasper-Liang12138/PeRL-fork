# vLLM + NPU 环境依赖
# 适用于华为昇腾NPU的GRPO训练环境
# 华为昇腾910B
# 基于 ubuntu22.04-teleformers-cann8.2.rc1-npu:v0.2.0.post1.ssh

# 核心深度学习框架
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1
torch-npu==2.9.0

# vLLM及其NPU支持
vllm==0.15.1
vllm-ascend==0.13.0

# Transformers和相关
transformers==4.57.6
tokenizers==0.22.2
accelerate>=0.20.0

# TRL，支持vLLM的版本（v0.18.0+开始支持）
trl==0.28.0 

# DeepSpeed
deepspeed>=0.14.0

# 数据处理
datasets==4.5.0
numpy==1.26.4
pandas>=2.0.0

# PEFT (参数高效微调)
peft>=0.8.0

# 其他依赖
opencv-python-headless==4.13.0.92
fsspec==2025.10.0
scipy>=1.10.0
scikit-learn>=1.3.0
wandb>=0.15.0
pyyaml>=6.0
tqdm>=4.65.0
packaging>=23.0

# NPU相关
triton-ascend==3.2.0

# 注意事项：
# 1. vllm-ascend 0.13.0 要求 torch==2.8.0，但我们使用 torch 2.9.1
#    这个版本冲突在实际使用中可以忽略，因为vllm-ascend插件已经能识别NPU
# 2. 如果遇到问题，可以尝试降级到完全兼容的版本组合
